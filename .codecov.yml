codecov:
  branch: main

comment:
  layout: "condensed_header, diff, flags, components"

component_management:
  individual_components:
    - component_id: dogdata-rs
      name: DogData
      paths:
        - crates/dogdata
    - component_id: dogdata-reqwest-middleware
      name: Reqwest Datadog Tracing
      paths:
        - crates/dogdata-reqwest-middleware
    - component_id: dogdata-sqlx
      name: SQLx Datadog Tracing
      paths:
        - crates/dogdata-sqlx
